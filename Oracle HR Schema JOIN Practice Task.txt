Objective
Build business reports using the Oracle HR Schema while demonstrating a strong understanding of table relationships, JOIN operations, and enterprise reporting requirements.
Scope
1. HR Database Exploration
- Explore the Oracle HR schema.
- Inspect the relevant tables.
- Identify relationships and foreign keys.
- Understand how employee information flows across the system.
- Relevant tables:
- EMPLOYEES
- DEPARTMENTS
- JOBS
- LOCATIONS
- COUNTRIES
- REGIONS


The Oracle HR Schema is designed to manage employee, department, job, and location information within an organization. It contains several related tables that support human resources reporting and organizational analysis.
EMPLOYEES
+ Stores employee information such as:

Employee ID
First Name
Last Name
Email
Hire Date
Salary
Job ID
Manager ID
Department ID

Primary Key:

EMPLOYEE_ID

Foreign Keys:

JOB_ID → JOBS.JOB_ID
MANAGER_ID → EMPLOYEES.EMPLOYEE_ID
DEPARTMENT_ID → DEPARTMENTS.DEPARTMENT_ID

DEPARTMENTS
+ Stores department information.

Attributes include:

Department ID
Department Name
Manager ID
Location ID

Primary Key:

DEPARTMENT_ID

Foreign Keys:

MANAGER_ID → EMPLOYEES.EMPLOYEE_ID
LOCATION_ID → LOCATIONS.LOCATION_ID

JOBS
+ Stores job information.

Attributes include:

Job ID
Job Title
Minimum Salary
Maximum Salary

Primary Key:

JOB_ID

LOCATIONS
+ Stores office location information.

Attributes include:

Location ID
Street Address
Postal Code
City
State Province
Country ID

Primary Key:

LOCATION_ID

Foreign Key:

COUNTRY_ID → COUNTRIES.COUNTRY_ID

COUNTRIES
+ Stores country information.

Attributes include:

Country ID
Country Name
Region ID

Primary Key:

COUNTRY_ID

Foreign Key:

REGION_ID → REGIONS.REGION_ID

REGIONS
Stores geographical region information.

Attributes include:

Region ID
Region Name

Primary Key:

REGION_ID
-------------------------
# Relationships and Foreign Keys

The EMPLOYEES table is related to the DEPARTMENTS table through the Department_ID column. Each employee belongs to one department.

The EMPLOYEES table is related to the JOBS table through the Job_ID column. Each employee is assigned to one job.

The EMPLOYEES table has a self-referencing relationship through the Manager_ID column. This relationship is used to identify the manager of each employee.

The DEPARTMENTS table is related to the EMPLOYEES table through the Manager_ID column, which identifies the employee who manages the department.

The DEPARTMENTS table is related to the LOCATIONS table through the Location_ID column. Each department operates in a specific location.

The LOCATIONS table is related to the COUNTRIES table through the Country_ID column. Each location belongs to a country.

The COUNTRIES table is related to the REGIONS table through the Region_ID column. Each country belongs to a specific geographical region.

These relationships allow employee information to be connected with jobs, departments, locations, countries, and regions, which supports enterprise reporting and business analysis.

-------------------------
# Employee Information Flow

Employee information flows through the HR system as follows:

Each employee is assigned a job through the JOB_ID column.
Each employee belongs to a department through the DEPARTMENT_ID column.
Each department operates at a specific location through LOCATION_ID.
Each location belongs to a country through COUNTRY_ID.
Each country belongs to a geographical region through REGION_ID.
Employees may report to another employee through the MANAGER_ID column, creating the organizational hierarchy.

This structure enables business reporting at multiple levels, including employee, department, city, country, and regional analysis.

Conclusion

The Oracle HR Schema uses primary keys and foreign keys to maintain data integrity and support enterprise reporting. Understanding these relationships is essential for writing accurate JOIN queries, creating analytical reports, and performing business intelligence analysis.
2. Employee & Department Reports
Create SQL queries for:
Employee ID, full name, and department name (employees with departments only).

SELECT E.Employee_ID, E.First_Name || ' ' || E.Last_Name AS Full_Name, D.Department_Name
FROM Employees E INNER JOIN Departments D
  ON E.Department_ID = D.Department_ID;
----------
Employee full name, job title, and salary (employees with valid jobs only).

SELECT E.First_Name || ' ' || E.Last_Name AS Full_Name, J.Job_Title, E.Salary
FROM Employees E INNER JOIN Jobs J
  ON E.Job_ID = J.Job_ID;
-----------
Employee, department, and department city.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, D.Department_Name, L.City
FROM Employees E INNER JOIN Departments D
    ON E.Department_ID = D.Department_ID
INNER JOIN Locations L
    ON D.Location_ID = L.Location_ID;
-------------
Employee, department, country, and region.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, D.Department_Name, C.Country_Name, R.Region_Name
FROM Employees E INNER JOIN Departments D
    ON E.Department_ID = D.Department_ID
INNER JOIN Locations L
    ON D.Location_ID = L.Location_ID
INNER JOIN Countries C
    ON L.Country_ID = C.Country_ID
INNER JOIN Regions R
    ON C.Region_ID = R.Region_ID;
----------------------------------------------------------------------------------------------------------------------------
3. Organization Structure Analysis
Create reports for:
5. Complete employee list including employees without departments.

SELECT E.Employee_ID, E.First_Name || ' ' || E.Last_Name AS Employee_Name, D.Department_Name
FROM Employees E LEFT JOIN Departments D
    ON E.Department_ID = D.Department_ID;
-------------
6. Complete department list including departments without employees.

SELECT D.Department_ID, D.Department_Name, E.First_Name || ' ' || E.Last_Name AS Employee_Name
FROM Departments D LEFT JOIN Employees E
    ON D.Department_ID = E.Department_ID;
--------------
7. Departments that currently have no employees assigned.

SELECT D.Department_ID, D.Department_Name
FROM Departments D LEFT JOIN Employees E
    ON D.Department_ID = E.Department_ID
WHERE E.Employee_ID IS NULL;
-------------------------------------------------------------------------------------------------------------------------
4. Management Hierarchy Analysis
Create reports for:
8. Employee name and direct manager name.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, M.First_Name || ' ' || M.Last_Name AS Manager_Name
FROM Employees E LEFT JOIN Employees M
    ON E.Manager_ID = M.Employee_ID;
-------------------------
9. Employees without managers and provide a brief explanation.

SELECT Employee_ID, First_Name || ' ' || Last_Name AS Employee_Name
FROM Employees
WHERE Manager_ID IS NULL;
-- brief explanation: Employees without managers are typically top-level executives such as the company president or CEO. Since they are at the highest level of the organizational hierarchy, they do not report to any other employee.
------------------------------------------------------------------------------------------------------------------------
5. Advanced Business Analysis
Create reports for:
10. Employees earning more than their managers.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, E.Salary AS Employee_Salary, M.First_Name || ' ' || M.Last_Name AS Manager_Name, M.Salary AS Manager_Salary
FROM Employees E INNER JOIN Employees M
    ON E.Manager_ID = M.Employee_ID
WHERE E.Salary > M.Salary;
---------------------
11. Departments where the department manager works within the same department.

SELECT D.Department_Name, E.First_Name || ' ' || E.Last_Name AS Manager_Name
FROM Departments D INNER JOIN Employees E
    ON D.Manager_ID = E.Employee_ID
WHERE D.Department_ID = E.Department_ID;
-----------------------------------------------------------------------------------------------------------------------
6. Executive Dashboard Reports
Create professional reports for:
Employee, job title, department, city, and country.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, J.Job_Title, D.Department_Name, L.City, C.Country_Name
FROM Employees E INNER JOIN Jobs J
    ON E.Job_ID = J.Job_ID
INNER JOIN Departments D
    ON E.Department_ID = D.Department_ID
INNER JOIN Locations L
    ON D.Location_ID = L.Location_ID
INNER JOIN Countries C
    ON L.Country_ID = C.Country_ID;
--------------------
Department, manager, city, and country (including departments without managers).

SELECT D.Department_Name, E.First_Name || ' ' || E.Last_Name AS Manager_Name, L.City, C.Country_Name
FROM Departments D LEFT JOIN Employees E
    ON D.Manager_ID = E.Employee_ID
LEFT JOIN Locations L
    ON D.Location_ID = L.Location_ID
LEFT JOIN Countries C
    ON L.Country_ID = C.Country_ID;
--------------------
Employees working in the same department as their manager.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, M.First_Name || ' ' || M.Last_Name AS Manager_Name, D.Department_Name
FROM Employees E INNER JOIN Employees M
    ON E.Manager_ID = M.Employee_ID
INNER JOIN Departments D
    ON E.Department_ID = D.Department_ID
WHERE E.Department_ID = M.Department_ID;
---------------------
Employees whose managers work in different departments.

SELECT E.First_Name || ' ' || E.Last_Name AS Employee_Name, M.First_Name || ' ' || M.Last_Name AS Manager_Name
FROM Employees E INNER JOIN Employees M
    ON E.Manager_ID = M.Employee_ID
WHERE E.Department_ID <> M.Department_ID;
----------------------
Distinct countries containing company departments.

SELECT DISTINCT C.Country_Name
FROM Departments D INNER JOIN Locations L ON D.Location_ID = L.Location_ID
INNER JOIN Countries C
    ON L.Country_ID = C.Country_ID
ORDER BY C.Country_Name;
